#include<stdio.h>
int main(){
  printf("size of integer=%ld",sizeof(int));
  printf("\nsize of long integer=%ld",sizeof(long int));
  printf("\nsize of short integer=%ld",sizeof(short int));
  printf("\nsize of float integer=%ld",sizeof(float));
  printf("\nsize of double integer=%ld",sizeof(double));
  printf("\nsize of char integer=%ld",sizeof(char));
  return 0;
}
